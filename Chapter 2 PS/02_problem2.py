# check the type of variable assigned using the input() function

a = input("enter the value of a :") #the input() will read state that type is <string> which is why we must write < int(input (-- code--))
print(type(a))