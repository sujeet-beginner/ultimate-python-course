# Set : Set is a collection of non-repetitve elements

S = {1, 5, 32, 99} # It creates the set  

# Empty-Set 
S1 = set() # as dict and set both uses curly braces so it is not possible to create the empty set by using curly braces hence we do these it in these way
           # Dont use s = {} as it will create an empty dicitionary


S2 = {1, 2, 44, 54, 66, 5, 5, 5, 54}
print(S2)  # Set doesnt print the repeated number again and again