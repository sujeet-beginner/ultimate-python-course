# Replace the double spaces from problem 3 with single spaces

name = "Harry is a good  boy and "

print(name.replace("  ", " "))    # we use replace() method to replace the find(" double space ") to "single space " 


# Strings are immutable which means that you cannot chnage them by running functions on them 